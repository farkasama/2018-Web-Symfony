{% extends 'inc.html.twig' %}

{% block body %}
    <div class="jumbotron text-center">
    <h1>WELCOME</h1>
    </div>
    {% for serie in series %}
        <a href="{{ path('show_serie', {'name' : serie.name, 'id' : serie.idEnsemble}) }}"> {{ serie.name }}</a>
        <br>
    {% endfor %}
{% endblock %}